# 树莓派5 / Mac Mini 端侧语音暴力检测系统 Python 依赖

# 核心依赖
numpy>=1.24.0
torch>=2.0.0
torchaudio>=2.0.0

# 音频处理
pyaudio>=0.2.14
opuslib>=3.0.0
scipy>=1.10.0

# 本地声纹特征提取 (ECAPA-TDNN)
speechbrain>=1.1.0

# Whisper ASR
openai-whisper>=20231117
# 或使用 Faster-Whisper (性能更好，CPU友好)
# faster-whisper>=0.9.0

# VAD (Voice Activity Detection)
# silero-vad (通过torch.hub加载)

# TTS (可选，需要网络)
edge-tts>=6.1.0

# Web 管理界面
Flask>=3.0.0
gunicorn>=22.0.0

# LLM / 阿里云 ASR / 阿里云 TTS
openai>=1.0.0
dashscope>=1.22.0

# SMB 录音归档上传
pysmb>=1.2.9

# 工具
PyYAML>=6.0
